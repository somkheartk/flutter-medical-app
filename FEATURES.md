# Features Documentation - เอกสารคุณสมบัติ

## 🎯 ภาพรวมโปรเจค

Medical App เป็นแอปพลิเคชันทางการแพทย์ที่พัฒนาด้วย:
- **Frontend**: Flutter (Mobile/Desktop/Web)
- **Backend**: NestJS (REST API)
- **Font**: Kanit (ฟอนต์ภาษาไทย)

## 📱 Flutter App Features

### 1. หน้าหลัก (Home Screen)
- แสดงข้อความต้อนรับภาษาไทย
- สถิติด่วน:
  - จำนวนผู้ป่วยทั้งหมด
  - นัดหมายวันนี้
- เมนูหลัก:
  - จัดการนัดหมาย
  - จัดการผู้ป่วย

### 2. หน้านัดหมาย (Appointments Screen)
- แสดงรายการนัดหมายทั้งหมด
- ข้อมูลแต่ละนัดหมาย:
  - ชื่อผู้ป่วย
  - วันที่และเวลา
  - ชื่อแพทย์
  - สถานะ (pending, confirmed, cancelled)
- ฟังก์ชัน:
  - เพิ่มนัดหมายใหม่
  - แก้ไขนัดหมาย
  - ลบนัดหมาย
  - รีเฟรชข้อมูล

### 3. หน้าผู้ป่วย (Patients Screen)
- แสดงรายการผู้ป่วยทั้งหมด
- ข้อมูลแต่ละผู้ป่วย:
  - ชื่อ-นามสกุล
  - อายุ
  - เพศ
  - เบอร์โทรศัพท์
- ฟังก์ชัน:
  - เพิ่มผู้ป่วยใหม่
  - แก้ไขข้อมูลผู้ป่วย
  - ลบผู้ป่วย
  - รีเฟรชข้อมูล

### 4. การใช้ Kanit Font
ฟอนต์ Kanit ถูกตั้งค่าเป็น default font ทั้งแอป:
```dart
theme: ThemeData(
  fontFamily: 'Kanit',
  // ...
)
```

รองรับน้ำหนักฟอนต์:
- Regular (400)
- Light (300)
- Medium (500)
- Bold (700)

## 🔧 NestJS Backend Features

### 1. Architecture
- **Modular Design**: แยก modules ตามฟีเจอร์
- **TypeScript**: Type-safe development
- **RESTful API**: Standard REST endpoints
- **CORS Enabled**: รองรับ cross-origin requests

### 2. API Endpoints

#### Health Check
```
GET /api/health
```
ตรวจสอบสถานะ backend

#### Appointments API
```
GET    /api/appointments        # ดูรายการนัดหมายทั้งหมด
GET    /api/appointments/:id    # ดูนัดหมายเฉพาะ ID
POST   /api/appointments        # สร้างนัดหมายใหม่
PUT    /api/appointments/:id    # แก้ไขนัดหมาย
DELETE /api/appointments/:id    # ลบนัดหมาย
```

#### Patients API
```
GET    /api/patients            # ดูรายการผู้ป่วยทั้งหมด
GET    /api/patients/:id        # ดูผู้ป่วยเฉพาะ ID
POST   /api/patients            # สร้างข้อมูลผู้ป่วยใหม่
PUT    /api/patients/:id        # แก้ไขข้อมูลผู้ป่วย
DELETE /api/patients/:id        # ลบข้อมูลผู้ป่วย
```

### 3. Data Models

#### Appointment Model
```typescript
{
  id: string;
  patientName: string;
  date: string;        // YYYY-MM-DD
  time: string;        // HH:MM
  doctor: string;
  status: 'pending' | 'confirmed' | 'cancelled';
}
```

#### Patient Model
```typescript
{
  id: string;
  name: string;
  age: number;
  gender: string;
  phone: string;
}
```

### 4. Sample Data
Backend มาพร้อมข้อมูลตัวอย่าง:
- 2 นัดหมาย
- 3 ผู้ป่วย

## 🔄 การทำงานร่วมกัน

1. Flutter App ส่ง HTTP requests ไปยัง Backend
2. Backend ประมวลผล และส่งข้อมูล JSON กลับ
3. Flutter App แสดงผลข้อมูลด้วย Kanit font

## 🚀 การพัฒนาต่อ (Future Enhancements)

### ฟีเจอร์ที่แนะนำเพิ่ม:
1. **Database Integration**
   - MongoDB
   - PostgreSQL
   - MySQL

2. **Authentication & Authorization**
   - JWT tokens
   - Role-based access control

3. **Additional Features**
   - ประวัติการรักษา
   - ใบสั่งยา
   - รายงานผลตรวจ
   - การนัดหมายซ้ำ
   - แจ้งเตือนผ่าน notifications

4. **UI Improvements**
   - Dark mode
   - Responsive design
   - Animations
   - Image upload สำหรับรูปโปรไฟล์

5. **Backend Enhancements**
   - Validation pipes
   - Error handling middleware
   - Logging
   - API documentation (Swagger)

## 📝 ข้อควรระวัง

- ข้อมูลจะหายเมื่อรีสตาร์ท backend (in-memory storage)
- ควรเพิ่ม validation สำหรับ production
- ควรใช้ environment variables สำหรับ configuration
- ควรเพิ่ม error handling ที่ดีขึ้น
- ควรเพิ่ม unit tests และ integration tests
